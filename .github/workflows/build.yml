name: Build & SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  build-and-scan:
    runs-on: ubuntu-latest
    env:
      SONAR_ORG: petkunindustries
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Build Wrapper
        uses: SonarSource/sonarqube-scan-action/install-build-wrapper@v6

      - name: Build Laba6
        run: |
          rm -rf Laba6/build
          mkdir Laba6/build
          cmake -S Laba6 -B Laba6/build
          build-wrapper-linux-x86-64 --out-dir Laba6/build_wrapper make -C Laba6/build
          
      - name: SonarCloud scan Laba6
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=PeTkun_Industries_Laba6
            -Dsonar.organization=${{ env.SONAR_ORG }}
            -Dsonar.sources=Laba6
            -Dsonar.cfamily.compile-commands=Laba6/build_wrapper/compile_commands.json
            -Dsonar.coverage.exclusions=**
            -Dsonar.cfamily.skipCoverage=true
